package hello.world;

import javax.inject.Named;
import javax.inject.Singleton;

@Singleton
@Named("helloServiceSpan")
public class HelloServiceSpan implements HelloService {

    @Override
    public String hello(String name) {
        return "Hola " + name;
    }
}
